[pytest]
testpaths =
    tests/unit
    tests/integration
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
markers =
    unit: Unit tests (pure functions, no external deps)
    integration: Integration tests (DB, API)
    slow: Slow tests (AI calls, network)
